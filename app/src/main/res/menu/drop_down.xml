<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/action_sign_out"
        android:orderInCategory="100"
        android:title="@string/action_sign_out"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        app:showAsAction="never" />

    <!-- consider using this button as a dark mode toggle to proc
            the built-in android functionality-->
    <item
            android:id="@+id/bt_switch"
            android:title = "Dark Mode" />

    <item
        android:id="@+id/action_leave_chat"
        android:title="Leave Chat"
        app:showAsAction="never"
        android:visible="false" />

    <item
        android:id="@+id/action_invite_friend_button"
        android:icon="@drawable/ic_add_friend_to_chat_24"
        app:showAsAction="always"
        android:visible="false"/>

</menu>