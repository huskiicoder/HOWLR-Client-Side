<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/popup_reset"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/text_reset_email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="30dp"
        android:text="Enter the email for password reset:"
        android:textSize="22sp"/>

    <EditText
        android:id="@+id/edit_email_reset"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ems="10"
        android:inputType="textEmailAddress"
        android:text="uwnetid5@fake.email.com"
        app:layout_constraintTop_toBottomOf="@+id/text_reset_email"
        app:layout_constraintStart_toStartOf="@+id/text_reset_email"
        app:layout_constraintEnd_toEndOf="@+id/text_reset_email"/>

    <Button
        android:id="@+id/button_submit_reset"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Submit"
        app:layout_constraintEnd_toEndOf="@+id/edit_email_reset"
        app:layout_constraintTop_toBottomOf="@+id/edit_email_reset"/>

</androidx.constraintlayout.widget.ConstraintLayout>